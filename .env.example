# Copy this file to .env and fill in your values
# Never commit the .env file — it is in .gitignore

# ── Gemini AI (required for smart field extraction) ──────────────────────────
# Get your key: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# ── Database (choose ONE) ─────────────────────────────────────────────────────

# Option A: Supabase PostgreSQL (recommended for cloud)
# Get this from Supabase dashboard → Settings → Database → Connection String → URI
# DATABASE_URL=postgresql://postgres:[password]@db.[project].supabase.co:5432/postgres

# Option B: SQLite (default, works out of the box locally)
# DATABASE_PATH=conversions.db

# ── Optional ──────────────────────────────────────────────────────────────────
# On Render.com set: DATABASE_PATH=/var/data/conversions.db  (persistent disk)
